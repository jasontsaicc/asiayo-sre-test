apiVersion: v1
kind: PersistentVolume
metadata:
  name: asiayo-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: "/mnt/data"  # 本地測試用, 實際需搭配 AWS EBS, NFS 或其他 CSI driver
